fun main() {
    /@ requires acc(heap[0..4]) @/
    /@ requires acc(heap[4..8]) @/
    /@ requires acc(heap[10..=12]) @/
    var 4 x = lds 4 @base;
    var 4 y = lds 4 @base + 4 * @biw;
    var 3 z = lds 3 @base + 10 * @biw;
    return 0;
}